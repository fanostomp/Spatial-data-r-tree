# Εργασία 2 — Χωρικά Δεδομένα

## 1. Εισαγωγή

Η εργασία αφορά την κατασκευή ενός R-tree για χωρικά δεδομένα σημείων και την αποτίμηση βασικών χωρικών ερωτημάτων πάνω στο δέντρο. Τα δεδομένα εισόδου αντιστοιχούν σε θέσεις εστιατορίων στο Πεκίνο. Κάθε σημείο περιγράφεται από δύο συντεταγμένες, `x` και `y`, ενώ το `record-id` του αντιστοιχεί στη θέση της εγγραφής στο αρχείο δεδομένων. Η πρώτη εγγραφή δεδομένων έχει `record-id = 1`.

Η υλοποίηση χωρίζεται σε δύο βασικά μέρη:

1. Κατασκευή του R-tree στη μνήμη με χρήση της τεχνικής **Sort-Tile-Recursive (STR)** και αποθήκευσή του σε αρχείο `rtree.csv`.
2. Επαναφόρτωση του `rtree.csv` και εκτέλεση ερωτημάτων **window range**, **distance range** και **k-nearest neighbours**.

## 2. Δομές δεδομένων

Το R-tree αναπαρίσταται ως συλλογή κόμβων με μοναδικά ακέραια `node-id`. Κάθε κόμβος περιέχει:

- `node_id`: το μοναδικό αναγνωριστικό του κόμβου,
- `is_leaf`: ένδειξη αν είναι φύλλο ή ενδιάμεσος κόμβος,
- `entries`: τις εγγραφές του κόμβου,
- `mbr`: το Minimum Bounding Rectangle του κόμβου.

Στα φύλλα, κάθε εγγραφή έχει λογική μορφή:

```text
(record-id, (x, y))
```

Στους ενδιάμεσους κόμβους, κάθε εγγραφή έχει μορφή:

```text
(child-node-id, [x-low, y-low, x-high, y-high])
```

Το MBR ενός φύλλου υπολογίζεται από τις ελάχιστες και μέγιστες συντεταγμένες των σημείων του. Το MBR ενός ενδιάμεσου κόμβου είναι το μικρότερο ορθογώνιο που περικλείει τα MBRs όλων των παιδιών του.

## 3. Χωρητικότητες κόμβων

Σύμφωνα με τις προδιαγραφές, κάθε κόμβος έχει χωρητικότητα 1024 bytes.

| Τύπος κόμβου | Μέγεθος εγγραφής | Μέγιστη χωρητικότητα | Ελάχιστη χωρητικότητα |
|---|---:|---:|---:|
| Φύλλο | 20 bytes | 51 εγγραφές | 20 εγγραφές |
| Ενδιάμεσος κόμβος | 36 bytes | 28 εγγραφές | 11 εγγραφές |

Η ελάχιστη χωρητικότητα αντιστοιχεί σε πληρότητα 40%. Κατά την ομαδοποίηση εφαρμόζεται έλεγχος underflow. Αν ο τελευταίος κόμβος ενός επιπέδου έχει λιγότερες εγγραφές από το ελάχιστο επιτρεπτό όριο, μεταφέρονται εγγραφές από τον προηγούμενο κόμβο. Η ρίζα αποτελεί εξαίρεση από τον περιορισμό ελάχιστης πληρότητας.

## 4. Κατασκευή του R-tree

Η κατασκευή εκτελείται από το `meros1.py`, το οποίο χρησιμοποιεί το πακέτο `spatial_rtree`.

### 4.1 Ανάγνωση δεδομένων

Η πρώτη γραμμή του αρχείου περιέχει το δηλωμένο πλήθος σημείων. Οι επόμενες γραμμές περιέχουν δύο αριθμητικές τιμές:

```text
x y
```

Για κάθε γραμμή δημιουργείται μία εγγραφή σημείου. Η refactored υλοποίηση ελέγχει ότι:

- η πρώτη γραμμή είναι έγκυρος ακέραιος,
- κάθε εγγραφή έχει ακριβώς δύο αριθμητικές συντεταγμένες,
- το δηλωμένο πλήθος συμφωνεί με τα σημεία που διαβάστηκαν,
- το αρχείο δεν είναι κενό.

### 4.2 Κατασκευή φύλλων με STR

Για την κατασκευή του επιπέδου των φύλλων εφαρμόζεται η τεχνική Sort-Tile-Recursive:

1. Τα σημεία ταξινομούνται ως προς τη συντεταγμένη `x`.
2. Υπολογίζεται το πλήθος των φύλλων που απαιτούνται:

   ```text
   P = ceil(N / M)
   ```

   όπου `N` είναι το πλήθος σημείων και `M = 51` η μέγιστη χωρητικότητα φύλλου.

3. Υπολογίζεται το πλήθος των κάθετων slices:

   ```text
   S = ceil(sqrt(P))
   ```

4. Τα ταξινομημένα σημεία χωρίζονται σε slices χωρητικότητας `S × M`.
5. Κάθε slice ταξινομείται ως προς τη συντεταγμένη `y`.
6. Τα σημεία πακετάρονται σε φύλλα έως 51 εγγραφών και διορθώνεται πιθανό underflow του τελευταίου φύλλου.

Μετά τη δημιουργία κάθε φύλλου υπολογίζεται το MBR του και δημιουργείται μία εγγραφή `(node-id, MBR)` για το επόμενο επίπεδο.

### 4.3 Κατασκευή ενδιάμεσων επιπέδων

Μετά την κατασκευή των φύλλων, το δέντρο χτίζεται bottom-up. Τα MBRs των κόμβων του προηγούμενου επιπέδου πακετάρονται με τη σειρά δημιουργίας τους σε ενδιάμεσους κόμβους χωρητικότητας έως 28 εγγραφών.

Η διαδικασία επαναλαμβάνεται μέχρι να παραμείνει ένας μόνο κόμβος, ο οποίος αποτελεί τη ρίζα. Αυτή η επιλογή διατηρεί την προσέγγιση της αρχικής εργασίας: STR χρησιμοποιείται στο επίπεδο των φύλλων, ενώ τα ανώτερα επίπεδα δημιουργούνται με απλό bottom-up packing.

## 5. Αποθήκευση του δέντρου

Το R-tree αποθηκεύεται στο `rtree.csv`. Παρότι το αρχείο έχει κατάληξη CSV, η μορφή του είναι η ειδική μορφή που ορίζει η εργασία:

```text
node-id , n , f , (ptr1,geo1) , (ptr2,geo2) , ... , (ptrn,geon)
```

όπου:

- `node-id` είναι το ID του κόμβου,
- `n` είναι το πλήθος εγγραφών,
- `f = 0` για φύλλο και `f = 1` για ενδιάμεσο κόμβο,
- `ptr` είναι `record-id` ή `child-node-id`,
- `geo` είναι σημείο ή MBR.

Παράδειγμα φύλλου:

```text
0 , 2 , 0 , (1,(39.7, 116.1)) , (2,(39.8, 116.2))
```

Παράδειγμα ενδιάμεσου κόμβου:

```text
10 , 2 , 1 , (0, [39.7, 116.1, 39.8, 116.2]) , (1, [39.8, 116.2, 39.9, 116.3])
```

Οι κόμβοι γράφονται bottom-up: πρώτα τα φύλλα, στη συνέχεια οι ενδιάμεσοι κόμβοι και τελευταία η ρίζα.

## 6. Στατιστικά του δέντρου

Για κάθε επίπεδο εμφανίζονται:

- το πλήθος κόμβων,
- το μέσο εμβαδό των MBRs.

Η μορφή εξόδου είναι:

```text
<count> nodes at level <level> with average MBR area <area>
```

Στο επίπεδο των φύλλων αναφέρεται μέσο εμβαδό `0.0`, σύμφωνα με τον τρόπο παρουσίασης που ζητείται στην εργασία για το επίπεδο αποθήκευσης των σημείων.

Στην αρχική εκτέλεση με το σύνολο `Beijing_restaurants.txt` καταγράφηκαν:

| Επίπεδο | Κόμβοι | Μέσο εμβαδό MBR |
|---:|---:|---:|
| 0 | 1020 | 0.0 |
| 1 | 37 | 0.014773812397702606 |
| 2 | 2 | 0.1640782578990009 |
| 3 | 1 | 0.3246387377100014 |

## 7. Φόρτωση και επικύρωση του R-tree

Το `meros2.py` διαβάζει το `rtree.csv` και ανακατασκευάζει το δέντρο στη μνήμη. Για την ασφαλή μετατροπή των εγγραφών χρησιμοποιείται `ast.literal_eval`.

Η refactored υλοποίηση επικυρώνει επιπλέον:

- τη σύνταξη κάθε γραμμής,
- το `node-id` και το flag του κόμβου,
- τη συμφωνία του δηλωμένου πλήθους `n` με τις πραγματικές εγγραφές,
- τη μορφή σημείων και MBRs,
- την ύπαρξη κάθε child node που αναφέρεται από ενδιάμεσο κόμβο,
- την απουσία διπλότυπων node IDs.

Επειδή η αποθήκευση είναι bottom-up, το `node-id` της τελευταίας μη κενής γραμμής θεωρείται η ρίζα.

## 8. Window Range Queries

Ένα window query έχει μορφή:

```text
[x-low y-low x-high y-high]
```

Ζητούνται όλα τα σημεία που βρίσκονται μέσα στο παράθυρο ή πάνω στο περίγραμμά του. Για ένα σημείο `(x, y)` ο έλεγχος είναι:

```text
x-low <= x <= x-high
and
y-low <= y <= y-high
```

Η αναζήτηση ξεκινά από τη ρίζα:

- Σε ενδιάμεσο κόμβο εξετάζεται μόνο ένα παιδί του οποίου το MBR τέμνει ή ακουμπά το query window.
- Σε φύλλο ελέγχεται κάθε πραγματικό σημείο.

Δύο MBRs δεν τέμνονται μόνο όταν το ένα βρίσκεται πλήρως αριστερά, δεξιά, πάνω ή κάτω από το άλλο. Με αυτή την απόρριψη αποφεύγεται η επίσκεψη σε υποδέντρα που δεν μπορούν να περιέχουν αποτελέσματα.

Η μορφή εξόδου είναι:

```text
query-line (count): record-id1,record-id2,...
```

## 9. Distance Range Queries

Ένα distance range query έχει μορφή:

```text
[x y epsilon]
```

Ζητούνται όλα τα σημεία των οποίων η Ευκλείδεια απόσταση από το query point `q` είναι μικρότερη ή ίση από `epsilon`.

Για πραγματικό σημείο `p`:

```text
dist(q, p) = sqrt((qx - px)^2 + (qy - py)^2)
```

Για ενδιάμεσο κόμβο χρησιμοποιείται η ελάχιστη δυνατή απόσταση του query point από το MBR του παιδιού. Αν το σημείο βρίσκεται μέσα στο MBR, η ελάχιστη απόσταση είναι 0. Διαφορετικά, υπολογίζεται η απόσταση από το πλησιέστερο σημείο του ορθογωνίου.

Ένα υποδέντρο εξετάζεται μόνο όταν:

```text
minimum_distance(q, MBR) <= epsilon
```

Η μορφή εξόδου είναι:

```text
query-line (count): record-id1,record-id2,...
```

## 10. k-Nearest Neighbour Queries

Στα kNN queries δίνεται query point και θετικός ακέραιος `k`. Ζητούνται οι `k` πλησιέστεροι γείτονες.

Η υλοποίηση χρησιμοποιεί **best-first search** με min-heap από το standard module `heapq`. Η ουρά προτεραιότητας περιέχει:

- ενδιάμεσους κόμβους με κλειδί την ελάχιστη απόσταση του query point από το MBR τους,
- πραγματικά σημεία με κλειδί την Ευκλείδεια απόστασή τους από το query point.

Η διαδικασία είναι:

1. Εισάγεται η ρίζα στην ουρά.
2. Αφαιρείται κάθε φορά το στοιχείο με τη μικρότερη απόσταση.
3. Αν είναι κόμβος, εισάγονται οι εγγραφές του στην ουρά.
4. Αν είναι σημείο, το `record-id` προστίθεται στα αποτελέσματα.
5. Η διαδικασία σταματά όταν έχουν βρεθεί `k` σημεία ή όταν αδειάσει η ουρά.

Η μορφή εξόδου είναι:

```text
query-line: record-id1,record-id2,...,record-idk
```

## 11. Αρθρωτή δομή του refactored project

Η αρχική λογική διαχωρίστηκε σε μικρότερες μονάδες:

```text
spatial_rtree/
├── bulk_load.py   # ανάγνωση σημείων, STR και κατασκευή δέντρου
├── constants.py   # χωρητικότητες κόμβων
├── models.py      # typed δομές δεδομένων
├── storage.py     # αποθήκευση και φόρτωση rtree.csv
├── queries.py     # window, distance και kNN queries
└── cli.py         # command-line interfaces
```

Τα `meros1.py` και `meros2.py` παραμένουν διαθέσιμα ως συμβατά entry points, ώστε οι εντολές της εργασίας να μην αλλάξουν.

## 12. Οδηγίες εκτέλεσης

Κατασκευή του R-tree:

```bash
python meros1.py Beijing_restaurants.txt rtree.csv
```

Window range queries:

```bash
python meros2.py window rtree.csv windowRangeQueries.txt
```

Distance range queries:

```bash
python meros2.py distance rtree.csv distanceRangeQueries.txt
```

k-nearest neighbours με `k = 10`:

```bash
python meros2.py knn rtree.csv NNQueries.txt 10
```

Με editable installation διατίθενται επίσης οι εντολές:

```bash
build-rtree Beijing_restaurants.txt rtree.csv
query-rtree window rtree.csv windowRangeQueries.txt
```

## 13. Έλεγχοι

Οι αυτοματοποιημένοι έλεγχοι καλύπτουν:

- ανάγνωση και validation των σημείων,
- underflow redistribution,
- STR packing χωρίς μεταβολή της αρχικής λίστας,
- κατασκευή δέντρου και επιλογή ρίζας,
- round-trip αποθήκευσης και φόρτωσης,
- window range queries με συμπερίληψη ορίων,
- distance range queries με συμπερίληψη της ακτίνας,
- best-first kNN,
- parsing query files και ακριβή μορφή εξόδου.

Εκτέλεση:

```bash
python -m pip install -e ".[dev]"
pytest
```

## 14. Συμπεράσματα

Η εργασία υλοποιεί ένα R-tree για χωρικά δεδομένα σημείων με STR bulk loading στο επίπεδο των φύλλων και bottom-up packing στα ενδιάμεσα επίπεδα. Το δέντρο αποθηκεύεται σε αρχείο και επαναφορτώνεται για την αποτίμηση τριών βασικών κατηγοριών χωρικών ερωτημάτων.

Η χρήση των MBRs επιτρέπει την απόρριψη ολόκληρων υποδέντρων που δεν μπορούν να περιέχουν αποτελέσματα. Έτσι, τα window και distance range queries αποφεύγουν την άσκοπη εξέταση όλων των σημείων, ενώ το best-first kNN εξερευνά πρώτα τις πιο υποσχόμενες περιοχές του χώρου.
